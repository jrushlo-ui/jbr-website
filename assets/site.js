const b=document.querySelector('.menu'),n=document.querySelector('.links');if(b&&n){const close=()=>{n.classList.remove('open');b.setAttribute('aria-expanded','false')};b.addEventListener('click',()=>{const o=n.classList.toggle('open');b.setAttribute('aria-expanded',String(o))});n.addEventListener('click',e=>{if(e.target.closest('a'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}const f=document.querySelector('#contact');if(f)f.addEventListener('submit',e=>{e.preventDefault();const s=document.querySelector('#status');if(s){s.textContent='Preview mode — secure form delivery will be connected before the final domain launch.';s.setAttribute('role','status')}});